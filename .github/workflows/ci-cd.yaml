# name: CI-CD

# on:
#   push:
#     branches: ["fadilfasa"]
#   pull_request:
#     branches: ["fadilfasa"]

# jobs:
#   ci:
#     name: CI (lint, test, build)
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Setup Node
#         uses: actions/setup-node@v4
#         with:
#           node-version: "20"
#           cache: "pnpm"

#       - name: Setup PNPM
#         uses: pnpm/action-setup@v4
#         with:
#           version: latest

#       - name: Install deps
#         run: pnpm install --frozen-lockfile

#       - name: Unit Tests
#         run: pnpm test -- --run

#       - name: Build
#         run: pnpm build

# docker:
#   name: Build & Push Docker Image
#   runs-on: ubuntu-latest
#   needs: [ci]
#   if: github.event_name == 'push' # hanya push yang publish image

#   steps:
#     - name: Checkout
#       uses: actions/checkout@v4

#     - name: Login DockerHub
#       uses: docker/login-action@v3
#       with:
#         username: ${{ secrets.DOCKERHUB_USERNAME }}
#         password: ${{ secrets.DOCKERHUB_TOKEN }}

#     - name: Setup Docker Buildx
#       uses: docker/setup-buildx-action@v3

#     # Cache supaya build lebih cepat
#     - name: Build & Push
#       uses: docker/build-push-action@v6
#       with:
#         context: .
#         push: true
#         tags: |
#           fadilfasa/lombokhalalroom:latest
#           fadilfasa/lombokhalalroom:${{ github.sha }}
#         cache-from: type=gha
#         cache-to: type=gha,mode=max

# deploy:
#   name: Deploy to VPS (pull, migrate, restart)
#   runs-on: ubuntu-latest
#   needs: [docker]
#   if: github.event_name == 'push'

#   steps:
#     - name: SSH deploy
#       uses: appleboy/ssh-action@v1.2.0
#       with:
#         host: ${{ secrets.VPS_HOST }}
#         username: ${{ secrets.VPS_USER }}
#         key: ${{ secrets.VPS_SSH_KEY }}
#         port: ${{ secrets.VPS_PORT }}
#         script: |
#           set -e

#           cd /opt/lhr

#           # Pull image terbaru
#           docker compose pull

#           # Jalankan migrasi DB sebelum restart service (AMAN)
#           # Sesuaikan "backend" dengan nama service backend di docker-compose.yml
#           # Sesuaikan command migrate dengan milik project backend-mu
#           docker compose run --rm backend npm run migrate up

#           # Restart service dengan image baru
#           docker compose up -d --remove-orphans

#           # Bersih-bersih image lama (opsional)
#           docker image prune -f
